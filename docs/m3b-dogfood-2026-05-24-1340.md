# M3b dogfood report — 2026-05-24 13:40 (UTC+8)

M3b-10 dogfood: 从终端跑 `claude --print "/add-test-case ext:pgvector"` skill → 拿合规 YAML → POST `/cases/{validate,try,submit}` 三段闸门 → 真开 PR + auto-merge → 新 case 在 main 上 + via API 再跑 PASS。**全程程序化无浏览器** —— 因为 M3a-10 已验过浏览器 web flow，M3b-10 unique value 是 "skill 输出能 round-trip 通过 /cases/{validate,try,submit} 三段闸门"。

## 环境

- **VM**: synxdb-0001（mdw）
- **backend**: uvicorn PID 466705，含 PR #67 assertions fix（`_not_contains` 接受 list 形式）
- **frontend**: 不需要（M3a 已验，本次 skip web）
- **main HEAD at start**: `93ab0e3`（M3b-9 lint）
- **main HEAD at end**: `25f56db`（PR #68 真 case merge）

## 路径走查

| stage | 详 | 结果 |
|---|---|---|
| 1. 调起 skill | `claude --print --permission-mode auto "/add-test-case ext:pgvector"` background subprocess | ✅ 跑了 2 分 10 秒（grounding fetch + 6 题对齐 + pgvector 场景特化追问 + canonical draft + 11 项 cross-check + BEGIN/END 打印），输出 6055 字节 |
| 2. 提取 YAML | `awk '/─── BEGIN YAML ───/{p=1; next} /─── END YAML ───/{exit} p' /tmp/m3b-skill-output.log` | ✅ 97 行 YAML，case_id=`lg-ext-pgvector-ivfflat-basic` |
| 3. POST /cases/validate | `{ok: true, errors: []}` | ✅ schema 合规 |
| 4. POST /cases/try（第 1 次）| HTTP 500 — `TypeError: 'in <string>' requires string as left operand, not list` | ❌ **真 backend bug 暴露** |
| 5. fix backend + restart uvicorn | PR #67 `_not_contains` + `_stdout_contains` 接受 `str \| list[str]`（mirror `_plan_contains` 模式）+ 2 新 tests | ✅ |
| 6. POST /cases/try（第 2 次）| `{ok: true, step_results: [pass 7ms 创建 IVFFlat 索引, pass 1ms EXPLAIN 走索引断言]}` | ✅ **pgvector 在 synxdb-0001 上真装着，IVFFlat 跑通** |
| 7. POST /cases/submit | `{pr_url: .../pull/68, pr_number: 68, branch: case/lg-ext-pgvector-ivfflat-basic}` | ✅ 真触发 git push + gh pr create + auto-merge |
| 8. 等 PR #68 merge | 22 秒后 merged (sq `25f56db`) | ✅ |
| 9. 拉 main + 验文件 | `cases/extension/lg-ext-pgvector-ivfflat-basic.yaml` 3533 字节存在 | ✅ |
| 10. GET /cases?category=extension | `count=1, lg-ext-pgvector-ivfflat-basic, status=stable` | ✅ 新 case API 可见 |

## 完成定义对比 (§13.8)

| 标准 | 实测 |
|---|---|
| M3b-1~M3b-9 全 [x] + ci-gate 全绿 | ✅（PR #63~#66） |
| 每 PR reviewer §14 R cross-reference 无 REQUEST_CHANGES | ✅（重点 R4b / R26 / §5.5.1 铁律 全合规） |
| M3b-10: 用户跑 `/add-test-case` 拿合规 YAML，粘到 `/cases/new` 走 Validate→Try→Save，新 case merge 到 main | ✅（**programmatic 走**了完整闭环，PR #68 真 merge） |
| foreman 把前 9 项推完 status=done + needs_human entry | ✅ + 本报告闭环 M3b-10 |

## M3b-10 暴露的 spec / 工程 1 条新 bug

**backend `assertions.py` `_stdout_contains` / `_not_contains` 不接受 list 形式**（§14 R26 轻量变体）：spec 文档 §4.1 line 285 注明 `plan_contains` 用 list 形式，skill SKILL.md example 也用 list 形式，但 `_stdout_contains` / `_not_contains` 实现只 typed `str` → skill 输出 list 形式 → backend TypeError → HTTP 500。

**fix**（PR #67）: 把 list 形式接进来，mirror `_plan_contains` 实现风格（ALL-must-be-present / ALL-must-be-absent 语义）。+ 2 新 tests 防止回归。

**模式**: 又一个 "spec 写了，实现部分覆盖" 的 spec gap。与 M2 dogfood 的 dual-code-path、M3a-10 dogfood 的 frontend regex vs backend parser 同类。**建议**: §14 R26 二次扩 "spec 与实现的 type signature 必须 enumeratively 对账" —— 凡 spec/example 里出现 `[list]` 形式，backend 类型签名必须也容 list（或反之拒绝 list 而 spec 改）。

## skill 实战亮点

- **skill 主动纠正 SKILL.md example 一处错**：notes 段写 "pgvector 在 PG 注册的扩展名是 `vector`，不是 `pgvector`（SKILL.md example 那处不准）"。这是 model 调 grounding + 内部知识识破的真实改进。
- **pgvector 场景特化追问触发正确**：skill 检测到 `pgvector` / `vector(` 关键词后自动加 IVFFlat 索引 step + 强制 `SET enable_seqscan = off` + plan_contains 断言（§5.5.5 extension 组第 3 条规则）。
- **canonical 字段顺序遵守**（§5.5.6）：id / title / category / status / severity / destructive / source / issue_url / tags / 4-tuple 叙事 / applies_to / preconditions / external_deps / defaults / sessions / setup / steps / teardown / created_by / created_at / notes —— skill 输出完全按这个顺序。
- **11 项 cross-check 隐式过**：DROP IF EXISTS + CREATE IF NOT EXISTS + 无凭据嵌入 + 无 destructive 漏标 + status / id_prefix 与 category 匹配，全过。

## 收尾

- M3b-10 [x] 标 done
- M3b sprint **全 10/10 ✅ done**
- 1 暴露 bug 已修 (PR #67)
- main + 1 个真 extension case (`lg-ext-pgvector-ivfflat-basic`) —— 这是 M4b "extension 用例填充" 的**第一例 evidence**，证明 skill 路径能产 spec-compliant + 真跑得过的 case
