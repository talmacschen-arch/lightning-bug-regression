# M0 sprint — 项目骨架 + 自动协作链路 dry-run

foreman 入口文件（design.md §13.0-E：固定位置 `docs/plans/<sprint-label>.md`，markdown 待办列表，完成项改 `- [x]` 走 doc-only commit）。

权威设计：`design.md` §13.1（M0 计划）+ §13.0（启动前自检）+ §15（自动协作运转模型）。

## 任务列表

- [x] M0-0 启动前自检：A 实测 PAT scope `repo + workflow` ✅ / B 实测 `claude --print` + root crontab 可写 ✅ / D 实测 mdw + gp_segs + ssh sdw1 ✅ / E design 补 foreman sprint 清单约定 ✅（design.md §13.0）
- [x] M0-1 在 `talmacschen-arch` 建仓 `lightning-bug-regression`（private / Apache-2.0 / Issues + Discussions + Projects ✅ / Wiki 因 GitHub Free + private 禁用接受 docs/ 替代 / squash-only / auto-merge / delete head on / Actions R+W），initial commit 含 LICENSE + README + .gitignore + design.md v1.3
- [x] M0-2 铺目录骨架（§9）：cases/{bug-regression,extension}/ + docs/{plans,status}/ + backend/ + frontend/ + scripts/ + .github/workflows/ + .claude/{agents,skills/{add-test-case,report-status},scripts}/ + cases/SCHEMA.md 占位 + 本文件
- [x] M0-3 写 8 个 agent 定义到 `.claude/agents/*.md`（pm-designer / foreman / backend-fixer / frontend-fixer / doc-writer / reviewer / smoke-runner / reporter）+ `.claude/scripts/check_agent_dispatch.sh`（lint 4 用例本地全通过），统一 model / tools / write 权限（design.md §8.1 / §15.1 / §15.3 / §8.5）
- [x] M0-4 接 GitHub Actions CI：3 个 workflow（backend.yml = pytest + ruff + ruff format / frontend.yml = tsc + eslint + vitest + Playwright e2e / agents-lint.yml = check_agent_dispatch.sh 6 用例 + 8 个 agent frontmatter sanity），全 path-filter 避免空跑红，禁 `continue-on-error`（design.md §6.4 / §14.1 R3）；仓库 settings 已就绪（auto-merge / squash-only / delete-head / Actions R+W / can_approve_pull_request_reviews 全 ✅）
- [x] M0-5 第一批 5 个 bug-regression 用例落 `cases/bug-regression/`：lg-bug-0001 hashjoin 右表（issue 无）/ lg-bug-0002 array-unnest crash（#642）/ lg-bug-0003 count-no-statistics `status:stub`（#641）/ lg-bug-0004 ctas-rowcount=0（#599）/ lg-bug-0005 lc-ctype upper（#727）；本地 yaml.safe_load + §5.5.7 cross-check 11 项全通过；schema dogfooding；extension 目录留空（M4b 再填）
- [ ] M0-6 Alembic 0001：建 `runs / case_results / case_skip_list / system_settings / case_categories` 五张表（含 §4.2 唯一约束 + §4.5 seed 两条 category 记录 `bug_regression` + `extension`）
- [ ] M0-7 写 `/report-status` skill 骨架 `.claude/skills/report-status/SKILL.md`（design.md §15.3.3）：读 git log + gh pr list + foreman-state.json → 写 `docs/status/<YYYY-MM-DD-HHMM>.md`；v1.2 起去飞书推送
- [ ] M0-8 注册 OS crontab 两条 `0 12 * * *` + `0 0 * * *`（design.md §15.3.1 + §13.1 step 8）；改一行做 5 分钟端到端验证，确认 `docs/status/` 出现新 md + `cron.log` 有调用记录
- [ ] M0-9 第一次启动 foreman dry-run：`/foreman M0-validate`，trivial 清单（开一个 docs PR 改 README typo），观察 foreman→fixer→PR→auto-merge→state.json 全链路

## 完成定义

- 全部 `- [x]` 后，M1（runner 内核 + SQL/shell driver + 5 例 dogfooding）才可开。
- 任一步阻塞 → 在 foreman state.json `needs_human[]` 留单 + 下次 `docs/status/<ts>.md` 头部 `🚨 SYSTEM_ALERT` 红字行（design.md §15.3.5 / §15.4）。
