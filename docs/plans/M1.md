# M1 sprint — backend MVP (runner kernel + drivers + API)

foreman 入口文件（design.md §13.0-E）。M1 = 后端 MVP：把 YAML 加载 / runner 执行 / SQLite store / 基本 API 全跑通；尾端在真集群上 dogfood 5 例 bug-regression case。

权威设计：`design.md`
- §5（后端目录 + API 表 + 执行引擎）
- §5.3 / §5.3.1 / §5.3.2 / §5.3.3（Jinja / ssh user / 异常处理）
- §4.1（YAML schema）+ §4.2（runs / case_results）+ §4.5（case_categories）

§14 预付教训 — 写代码时回看：**R5**（超时分层）/ **R9**（step 异常 try/except 转 StepResult，不冒泡，Run 32 forensic）/ **R10**（cat >> 不 cat >，M4 extension 用，写 docstring 提醒）/ **R11**（远端 step 显式 source profile.d）/ **R12**（warmup retry）/ **R13**（ssh user 决策：host ∈ dut_hosts → gpadmin / 否则 root，Run 47 forensic）。

reviewer 必须按 §14 cross-reference（详 `.claude/agents/reviewer.md` step 5）。

## 任务列表（按依赖图 + 优先级）

### Sprint M1a — storage + drivers（首选；可并行）✅ done 2026-05-23

- [x] M1-1 `backend/pyproject.toml` 加 runtime deps — PR #2 (879e89f)
- [x] M1-prep `backend/app/runner/types.py` 共享 StepResult / StepStatus / StepError — PR #3 (d502aa9)（foreman 自加的预备 task）
- [x] M1-2 `backend/app/storage/yaml_loader.py` — PR #6 (19c9d20)
- [x] M1-3 `backend/app/storage/sqlite_store.py` — PR #5 (1cca90e)
- [x] M1-4 `backend/app/runner/assertions.py`（19 expect-clause evaluators + dispatcher）— PR #4 (19d6090)
- [x] M1-5 `backend/app/runner/sql_driver.py`（psycopg3 + NOTICE 捕获 + 双保险 timeout）— PR #10 (3726b82)
- [x] M1-6 `backend/app/runner/shell_driver.py`（asyncio subprocess + R9 异常折叠）— PR #8 (ea78235) **+ Phase 2 follow-up PR #11 (81e46ec) 修 2 个 Ubuntu/dash 真 bug**
- [x] M1-7 `backend/app/runner/log_grep_driver.py` — PR #7 (6a08abb)
- [x] M1-8 `backend/app/runner/jinja_render.py`（StrictUndefined + decide_ssh_user）— PR #9 (d3f8821)

### Sprint M1b — orchestrator + API（依赖 M1a 全 done）

- [x] M1-9 `backend/app/runner/orchestrator.py`：顺序跑 steps；不同 `on:` session 的 step 用 asyncio.gather 并发；teardown best-effort（R9 后续）；suite 内 destructive=true 排到最后（§5.3.3）；step 抛异常 → StepResult.error 不冒泡；第一个非-pass step 之后 break 但 teardown 仍跑；服务端日志检测到 `the database system is in recover mode` 强制中止 + 标 `cluster_crashed` — PR #12 (4239644)
- [x] M1-10 `backend/app/api/{runs,cases}.py + main.py`：FastAPI app；endpoint 集合 = `POST /runs`（409 on 已有 active）/ `GET /runs` / `GET /runs/{id}` / `GET /cases?category=&q=` / `GET /cases/{id}`；SSE `/runs/{id}/stream` 推到 M5；`/admin/categories` 读 case_categories 表（前端 / skill grounding 用）；OpenAPI 自动生成 — PR #13 (8bb6d5f)

### Sprint M1c — dogfood smoke（依赖 M1b 全 done）

- [x] M1-11 在 mdw=synxdb-0001 集群上用 runner 真跑 5 例 bug-regression：`lg-bug-0001~0005`；3 例 `status:open` 跑出来的状态与 BUG 实际未修状态对齐；`lg-bug-0003` stub 自动 skip；run record 落 SQLite；artifacts 落 `artifacts/<run_id>/<case_id>/`；产 run 总结 markdown 到 `docs/status/m1-dogfood-<ts>.md` — PRs #14 (5915ab9 dogfood script) + #15 (e80dc65 plan_contains fix) + smoke run 2026-05-23T17:23Z → docs/m1-dogfood-2026-05-23-172355.md (3 pass / 1 fail / 1 error)

## 完成定义

- **M1a 全 [x]** + pytest 覆盖率 80%+（backend 子模块）+ CI 全绿
- **M1b 全 [x]** + httpx 集成测试覆盖 happy-path 所有 endpoint + 409 路径
- **M1c [x]** + run record 1 条 + 5 例 status 全部对齐预期 + artifacts 落盘
- 总长度：foreman 分多个 session 跑（单 session 10 round / 2h budget）；中间 budget 用尽时 foreman 退出，下一次 invocation 从 docs/plans/M1.md 当前状态接力

## 失控防护

按 design.md §15.4 + foreman.md hard rules。重点：
- 同 symptom hash fail 2 次 → escalate `needs_human[]`
- 10 round 或 2h budget → BUDGET-EXHAUSTED 退出
- 阻塞类（集群不可达 / GH_TOKEN 失效 / 数据库锁住 / 远端依赖未就绪）写 needs_human 不重试
- backend.yml CI 已实战通过（M0 step 6 + 8 multiple runs），从 M1-1 起每个 PR 都触发完整 pytest+ruff
