"""Operational scripts (CLIs that drive the runner against real clusters).

These are NOT imported by the API/orchestrator; they are standalone CLI
entry points invoked via `uv run python -m backend.scripts.<name>`.
"""
