id: lg-bug-0003-count-no-statistics
title: "select count(*) 提示列无统计信息，analyze 也不解决"
category: bug_regression
status: stub
severity: medium
destructive: false

source:
  feishu_anchor: "lightning-2.2-2.3-upgrade-regression"
  reported_at: "2025-12-01"
  fixed_version: ""
  ext_doc_url: ""
issue_url: https://code.hashdata.xyz/field-engineering/dev_collaboration/-/issues/641
tags: [optimizer, statistics, count]

description: |
  跑 `SELECT count(*)` 时优化器/执行器提示 "one or more columns in the
  following tables do not have statistics"，对相关表 ANALYZE 也不消除该
  warning。stub：当前仅录入元信息，等本机能复现后再补可执行 SQL。
procedure: |
  待补：飞书原文未给出可独立复现的最小 SQL；首次手工 reproduce 通过后
  把触发 SQL（含表 DDL / 数据规模 / 是否分区 / 是否 mirror）落进 steps，
  并把 status 从 stub 升到 open。
expected: |
  - count(*) 正常返回；
  - 不出现 "one or more columns ... do not have statistics" 提示。

applies_to: {}
preconditions: {}
external_deps: []

defaults:
  database: postgres

# stub：steps 留空；runner 见 status:stub 会 skip 并在报告里标"待补可执行 SQL"。
steps: []

created_by: chenqiang
created_at: "2026-05-23"
notes: |
  停留在 stub 的理由（与 Q13 一致）：可执行 SQL 缺失 ≠ 修复状态未知；这两件
  事正交。等手工复现拿到稳定 trigger 后单独 PR 把本文件改成 status:open
  + 完整 steps。
